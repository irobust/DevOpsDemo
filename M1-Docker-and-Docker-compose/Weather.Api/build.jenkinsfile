def image
pipeline{
    agent any
    stages {
        stage('Build'){
            steps{
                // docker build -t irobust/weatherapi:jenkins -f dotnetcore.dockerfile M1-Docker-and-Docker-compose/Weather.Api/.
                image = docker.build("irobust/weatherapi:jenkins", "-f dotnetcore.dockerfile M1-Docker-and-Docker-compose/Weather.Api")
            }
        }
    }
}